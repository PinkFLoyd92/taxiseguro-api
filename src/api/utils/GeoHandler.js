const User = require('../models/user.model');
const Route = require('../models/route.model');

/* Si se encuentra un conductor, se lo asigna a dicho cliente, sino enviamos null */
const chooseDriver = (route, mapSockets = Map(), drivers = []) => {
  return null;
};

module.exports = chooseDriver;
